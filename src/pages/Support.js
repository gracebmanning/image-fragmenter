import Layout from "../layouts/layout";
export default function Support() {
    const body = <main className="w-full bg-neutral-400 flex flex-col flex-grow items-center justify-center p-4">support placeholder</main>;
    return <Layout body={body} />;
}
